{
  "name": "market-streamer",
  "version": "1.0.0",
  "description": "Market data streamer",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "esbuild index.js --bundle --platform=node --outfile=dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "redis": "^4.0.0",
    "ws": "^8.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.12"
  }
}
